# RUST TRAINING PLAYGROUND
started out using [rust-quiz](https://dtolnay.github.io/rust-quiz/1) , then translated a couple of the quizzes from the rust book and added some custom ones, may add more later.

to run:
- cargo build
- cargo run build
- cargo run -- serve

or run the rust-forge-gym.bat script after cargo build...


