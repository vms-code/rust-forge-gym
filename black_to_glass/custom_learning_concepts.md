# THINGS I THINK I WILL PROBABLY PRACTICALLY DO ALL THE TIME
1. loop over a vector or array and either change it's elements or values inside the elements.
2. changing data inside structs.
3. implementing traits that change one type into another
4. changing array shapes, doing math with matrices and vectors
5. manipulating strings
6. reading and writing to byte buffers
7. checking what happens when multiple traits implement the same function for the same type (how does compiler resolve?)
8. understanding how compiler resolves dereference
9. checking if it's possible to type cast, e.g. C++ patterns to rust ones
10. closure, scope and ownership on control flow blocks
11. when to constrain generics using 'where' ?
12. allocating and copying memory (freeing/drop mechanics too)
13. atomics, mutexes, multithreading in general
14. async mechanics (futures)
15. exactly how Result and Option work, when to use each
16. match resolution
17. macro expressions and how compiler resolves/sees them


